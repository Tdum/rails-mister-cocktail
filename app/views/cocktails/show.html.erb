<%= @cocktail.name %>
<%= link_to "Add a dose", new_cocktail_dose_path(@cocktail) %>
<%= @cocktail.doses.each do |d| %>
  <p><%= d.description  %></p>
  <%= link_to "Destroy a dose", dose_path(d), method: :delete %>
<% end %>

